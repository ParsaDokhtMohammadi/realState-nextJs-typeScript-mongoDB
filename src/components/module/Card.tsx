import styles from "@/components/module/Card.module.css"
import { IProfile } from "@/types/Interfaces"
const Card = ({data}:{data:IProfile}) => {
  return (
    <div>

    </div>
  )
}

export default Card 